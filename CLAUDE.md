# CLAUDE.md — راهنمای پروژه‌ی بازی کره زمین (The Earth Game)

این فایل برای دستیار Claude است تا هنگام کار روی این پروژه از آن پیروی کند.

---

## 🔴 مهم‌ترین قانون: استفاده از Code Graph

**برای هر گونه تحلیل کد، جستجو، فهم وابستگی‌ها و تغییر کد، باید از «code graph» (گراف کد) استفاده شود.**

- **در شروع کار (هر سشن):** ابتدا code graph را **سینک (sync)** کن تا با آخرین وضعیت کد هماهنگ باشد.
- **بعد از هر تغییری در کد:** دوباره code graph را **سینک کن** تا گراف به‌روز بماند و تحلیل‌های بعدی درست باشند.
- تحلیل ساختار، یافتن توابع/کلاس‌ها، ردیابی وابستگی‌ها و تأثیر تغییرات باید از طریق code graph انجام شود، نه صرفاً جستجوی متنی.

> خلاصه: **قبل از شروع → sync. بعد از تغییر کد → sync.** همیشه گراف کد را به‌روز نگه دار.

---

## معرفی پروژه

ربات تلگرامی یک **بازی استراتژیک متنی** (شبیه Hearts of Iron IV) در سال ۲۰۲۶.
هر بازیکن رهبر یک کشور است و بازی حول سه محور **اقتصاد، دیپلماسی، نظامی** می‌چرخد.
دستورهای آزاد بازیکن با **هوش مصنوعی (Groq / مدل `gpt-oss-120b`)** تفسیر و داوری می‌شوند.

- پلی‌بوک اصلی بازی: `C:\Users\ARASH\Game.txt`
- مخزن گیت‌هاب: https://github.com/Stranger0207/The-Earth-Game

---

## استک فنی

- **Python 3.11+** (تست‌شده روی 3.14)
- **aiogram 3.x** — فریم‌ورک تلگرام (async، مبتنی بر Router)
- **PostgreSQL** + **SQLAlchemy 2.0 (async)** + درایور **asyncpg**
- **APScheduler** — کارهای زمان‌دار (تیک هر ۶۰ ثانیه)
- **openai** library متصل به **Groq** (`base_url` گرو)
- **pydantic-settings** — کانفیگ از `.env`

---

## قواعد کدنویسی (مهم)

- **کد و نام متغیر/تابع: انگلیسی.**
- **کامنت‌ها: فارسی** (تا مالک پروژه متوجه شود).
- **متن‌های رو‌به‌کاربر (پیام‌های ربات): فارسی.**
- از enumها و ثابت‌ها به‌جای رشته‌های سحرآمیز استفاده کن (`bot/enums.py`, `bot/constants.py`).
- منطق بازی در `services/` (مستقل از تلگرام، قابل‌تست)؛ هندلرها فقط لایه‌ی تلگرام.

---

## ساختار پروژه

```
bot/
├── config.py            تنظیمات از .env (pydantic-settings)
├── loader.py            نمونه‌های Bot/Dispatcher
├── enums.py             شمارش‌گرها و نام‌های فارسی (منابع، تحریم‌ها، حملات، ...)
├── constants.py         اعداد بازی (هزینه‌ها، بازدهی‌ها، کول‌داون‌ها، اثرات اقتصادی)
├── database/
│   ├── base.py          engine + session + init_db (شامل مهاجرت خودکار ستون‌ها)
│   ├── models/          مدل‌های ORM (هر دامنه یک فایل)
│   └── repositories/    لایه‌ی دسترسی داده (CRUD هر دامنه)
├── services/            منطق بازی
│   ├── ai/              client (Groq)، context (DB→JSON)، prompts، evaluators
│   ├── economy_service.py     ساخت تأسیسات، فروش، اثرات اقتصادی، تعرفه
│   ├── military_service.py    فرمت تلفات و اعمال خسارت
│   ├── sanction_service.py    اثرات تحریم
│   ├── season_service.py      ریست کامل فصل
│   └── news_service.py        ارسال خبر به کانال‌ها و لاگ
├── handlers/            روترهای aiogram (start, claim, admin, menu, economy, diplomacy, military, advisor)
├── keyboards/           کیبوردهای inline
├── states/              وضعیت‌های FSM (فرم‌های چندمرحله‌ای)
├── middlewares/         تزریق session دیتابیس + کنترل کاربر/بن
├── scheduler/jobs.py    کارهای زمان‌دار (بازدهی، محموله، حمله، نشست، تماس)
└── utils/               فرمت اعداد فارسی و رندر پنل‌ها
data/
├── countries.json       داده‌ی پایه‌ی ۳۶ کشور (اقتصاد/ذخایر/نظامی) — منبع حقیقت برای seed و ریست
└── factions.json        گروهک‌ها (اطلاعاتی)
scripts/
├── seed.py              ساخت جدول‌ها + بارگذاری کشورها (+ --refresh-military)
├── migrate.py           مهاجرت دستی (معمولاً لازم نیست؛ init_db خودکار انجام می‌دهد)
└── import_military_v13.py   ایمپورت تجهیزات نظامی از فایل متنی
```

---

## دستورهای اجرا

```bash
pip install -r requirements.txt        # نصب وابستگی‌ها
cp .env.example .env                    # پر کردن مقادیر واقعی
python -m scripts.seed                  # ساخت جدول‌ها + ۳۶ کشور (یک‌بار)
python -m bot                           # اجرای ربات
```

دستورهای کمکی:
```bash
python -m scripts.seed --refresh-military   # تازه‌سازی تجهیزات از countries.json روی DB موجود
python -m scripts.migrate                    # مهاجرت دستی ستون‌ها (init_db خودکار هم انجام می‌دهد)
```

---

## نکات دیتابیس و مهاجرت

- `init_db()` در `bot/database/base.py` هنگام راه‌اندازی، جدول‌های جدید را می‌سازد و
  **ستون‌های جدید را به‌صورت خودکار به جدول‌های موجود اضافه می‌کند** (لیست `_COLUMN_MIGRATIONS`).
- **هر بار که ستونی به یک مدل اضافه کردی، حتماً آن را به `_COLUMN_MIGRATIONS` هم اضافه کن**
  تا روی دیتابیس‌های قدیمی (production) هم خودکار اعمال شود.
- `countries.json` منبع حقیقت برای داده‌ی اولیه است؛ ریست فصل (`season_service`) از روی آن بازسازی می‌کند.

---

## تست (بدون نیاز به PostgreSQL/توکن واقعی)

برای تست منطقی، از SQLite درون‌حافظه‌ای/فایل با env آزمایشی استفاده کن:
```bash
BOT_TOKEN="123456:ABC-test" DATABASE_URL="sqlite+aiosqlite:///./test.db" \
GROQ_API_KEY="gsk_test" OWNER_IDS="111" ADMIN_IDS="111" PYTHONUTF8="1" python -c "..."
```
- همیشه `PYTHONUTF8=1` بگذار (کنسول ویندوز با ایموجی فارسی مشکل دارد).
- بعد از هر تغییر: import کل ماژول‌ها + راه‌اندازی dispatcher را چک کن، و در صورت امکان فلوی منطقی را با SQLite تست کن.

---

## جریان کار گیت (Git)

- بعد از هر آپدیتِ تأییدشده: یک **commit** با پیام فارسیِ توصیفی بزن.
- پیام commit را با این خط پایان بده:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- سپس `git push origin main` (اتصال remote از قبل برقرار است).
- هر آپدیت = یک نقطه‌ی بازگشت؛ اگر چیزی خراب شد می‌توان به commit قبلی برگشت.

---

## ویژگی‌های کلیدی پیاده‌سازی‌شده (مرجع سریع)

- **کشورگیری:** `/claim` → تأیید مالک (`handlers/claim.py`, `admin.py`)
- **اقتصاد:** پنل، ذخایر، احداث معدن/کارخانه/سکو، فروش (با کول‌داون ۶ساعته)، اثر بر تورم
- **WTO:** محموله با زمان رسیدن تخمینی AI (`scheduler` تحویل می‌دهد)
- **دیپلماسی:** نامه، تماس ۵دقیقه‌ای، دیدار دوجانبه/چندجانبه (با پرواز)، قرارداد، تحریم (۹ نوع)، سخنرانی + نقل قول
- **چت و قرارداد در نشست:** هنگام دیدار فعال، پیام‌ها بین حاضران رله می‌شود و `/contract` فعال است
- **نظامی:** پنل تجهیزات، ۴ نوع حمله؛ نتایج به **کانال لاگ** می‌رود (نه کانال نظامی)؛ خرابکاری با تأیید مالک
- **تعرفه:** انحصاری آمریکا — کسر از معاملات و واریز به خزانه (`econ:tariffs`)
- **مشاور AI:** هر ۲۴ ساعت یک‌بار در هر دامنه
- **پایان فصل:** `/endseason` (فقط مالک) → ریست کامل به حالت اولیه
- **اعلان عمومی (v1.6):** `/announce متن` (مالک/مدیر) → ارسال به همه‌ی کشورهای دارای مالک
- **عکس در اخبار (v1.6):** سرویس `services/media.py` عکس تصادفی از فولدرهای `D:\PictureDB\{WTO,Trump,Diplomaci,Didar}` می‌فرستد و `file_id` تلگرام را در `File.md` کش می‌کند (بعد از اولین ارسال، نیازی به سیستم محلی نیست).
- **کارخانه نظامی (v1.7):** بخش نظامی → «کارخانه نظامی» → احداث/کارخانه‌های من. ۱۲ نوع کارخانه برای بازتولید تجهیزات موجود کشور (هزینه‌ی دلاری + منابع ساخت + مصرف هر چرخه؛ ثابت‌ها در `constants.py`). بازدهی هر ۲۴ ساعت (ناوچه/ناوشکن هر ۶ روز) توسط scheduler به موجودی اضافه می‌شود. احداث فقط در گروه لاگ اعلام می‌شود (نه کانال).
- **فروش تسلیحات (v1.7):** بخش نظامی → «فروش تجهیزات» → دسته/قلم/تعداد/قیمت/خریدار. پس از تأیید، محموله‌ی نظامی با عکس در کانال WTO اعلام و هنگام رسیدن به موجودی خریدار افزوده می‌شود (`MilitarySale`).
- **تأسیسات (v1.7):** خبر احداث تأسیسات فقط در پیوی و گروه لاگ (نه کانال)؛ دکمه‌ی «تأسیسات من» در بخش احداث.
- **تحریم (v1.7):** منوی تحریم با ۴ گزینه (وضع/وضع‌شده/تحریم‌های من/لغو). تحریم فردی حذف شد. **فقط یک خبر** تحریم با عکس مخصوص هر نوع از `D:\PictureDB\Embargo` در کانال دیپلماسی + لاگ (v1.9: خبر متنی تکراری حذف شد). برای کش‌کردن یک‌بارهٔ عکس‌های تحریم روی سیستم دارای فولدرها: `python -m scripts.cache_media`.
- **خبررسانی v1.6:** WTO هنگام حرکت محموله (عکس + فرمت جدید) و لاگ تجارت؛ رسیدن محموله فقط در پیوی خریدار و گروه لاگ. تعرفه: لاگ وضع تعرفه + لاگ عوارض به آمریکا/گروه لاگ + خبر ترامپ در کانال اقتصاد. دیدارها: خبر سفر و شروع نشست (دوجانبه/چندجانبه) با عکس، و رله‌ی صحبت‌های نشست به گروه لاگ.

---

## ویژگی‌های v1.9 (آپدیت بزرگ)

- **اتحادها:** بخش دیپلماسی → «اتحاد». فقط کشورهای VIP (`is_vip`) می‌توانند اتحاد بسازند و حداکثر `ALLIANCE_MAX_MEMBERS=6` کشور بیاورند. پنل: «اتحاد من» (جزئیات + مالک + مفاد + خروج) و «ایجاد اتحاد جدید». مدل‌ها: `Alliance`, `AllianceMember`؛ repo: `alliances`؛ handler: `alliance.py`.
- **نامه‌رسان:** بخش دیپلماسی → «ارسال نامه» سه گزینه: نامه به یک کشور / به چند کشور / صندوق پستی. زیر هر نامه دکمه‌ی «پاسخ به نامه»؛ صندوق پستی وضعیت پاسخ‌داده/بی‌پاسخ را نشان می‌دهد. مدل `Letter`؛ repo `letters`؛ handler `mail.py` (مالک `dip:letter`؛ هندلرهای قدیمی نامه از `diplomacy.py` حذف شد).
- **تأسیسات مشترک:** بخش احداث تأسیسات → «تأسیسات مشترک». انتخاب شریک → نوع → درصد شریک → محل → درخواست به شریک؛ با تأیید، هزینه به نسبت تقسیم (سهم شریک = درصد) و بازدهی هر ۲۴ ساعت با همان نسبت بین دو کشور تقسیم می‌شود. ستون‌های `partner_country`,`partner_percent` روی `facilities` (در `_COLUMN_MIGRATIONS`)؛ مدل `JointBuildRequest`؛ سرویس `build_joint_facility`؛ handler `joint.py`.
- **بانک:** بخش اقتصاد → «بانک»: موجودی، بدهی (+پرداخت با تأیید)، انتقال وجه (کشور→مبلغ→تأیید)، وام (غیرفعال). از `budget`/`govt_debt` استفاده می‌کند؛ handler `bank.py`.
- **سرمایه‌گذاری:** بخش اقتصاد → «سرمایه‌گذاری»: داخلی (روی خود) یا خارجی. ۱۰ دسته با درصد سود ۲۴ساعته در `INVESTMENT_CATEGORIES`. اصل سرمایه هنگام ثبت از بودجه کسر می‌شود؛ سود نقدی هر ۲۴ ساعت به **سرمایه‌گذار** می‌رسد (در `scheduler.process_investments`). سرمایه‌گذاری خارجی روی کشور هدف اثر اجتماعی دارد (+رضایت/-بیکاری/-تورم) و خبرش در کانال اقتصاد منتشر می‌شود؛ داخلی منتشر نمی‌شود. مدل `Investment`؛ handler `investment.py`.
- **پیام بازدهی:** هنگام بازدهی ۲۴ساعته‌ی تأسیسات/کارخانه‌ی نظامی/سرمایه‌گذاری، ربات به مالک پیام «سود/تولید + زمان بعدی» می‌فرستد (در `scheduler`).
- **اعلانات (تفکیک):** `/announce متن` → به همه (سازگاری قبلی)؛ `/announce` بدون متن → منوی «به همه / به یک‌چند کشور» (انتخاب چندتایی). `AnnounceForm` در `admin.py`.
- **خبر رفع تحریم:** هنگام لغو تحریم، خبر در کانال دیپلماسی + اطلاع به کشور هدف + لاگ.
- **کول‌داون‌ها (v1.9):** دیدار حضوری ۱/۳ساعت، بیانیه ۱/۱۰دقیقه، تماس ۱/۳۰دقیقه (با repo `cooldowns`). محدودیت ساخت به‌تفکیک نوع در ۱۲ ساعت: معدنی ۵، فولاد ۲، دکل نفت/گاز ۳ (مشترک)، کارخانه نظامی ۲ (`BUILD_LIMIT_GROUPS`, `MIL_FACTORY_BUILD_LIMIT`).
- **انحصار نشست‌ها (v1.10):** کشور تا پایان نشست/تماس فعال نمی‌تواند تماس/دیدار/نشست جدید بسازد یا بپذیرد؛ شامل حالت در پرواز (`get_active_engagement_label`).
- **ممیزی لاگ:** همه‌ی اقدامات کشورها (نامه، بیانیه، قرارداد، بانک، سرمایه‌گذاری، اتحاد، تأسیسات مشترک، اعلان، رفع تحریم و ...) در گروه لاگ ثبت می‌شوند.

## بازسازی سیستم نشست + اصلاحات (v1.10.1)

- **پرواز ثابت:** زمان سفر برای هر دیدار دوجانبه/چندجانبه ثابت **۳۰ دقیقه** است (دیگر بر اساس فاصله/AI نیست). ثابت `FLIGHT_DURATION_MINUTES=30`. تابع `evaluators.estimate_travel_time` دیگر در نشست‌ها استفاده نمی‌شود.
- **انحصار کامل در پرواز:** از لحظه‌ی پذیرش (وضعیت `ACTIVE` = در حال پرواز) تا پایان نشست، هر دو کشور قفل‌اند؛ هنگام **درخواست** هم اگر طرف مقابل مشغول باشد اصلاً درخواست ارسال نمی‌شود (چک `get_active_engagement_label` روی هدف در `cb_meet_to`/`cb_call_to`؛ در دعوت چندجانبه کشورهای مشغول دعوت نمی‌شوند).
- **پایان نشست (۲ راه + ۲ بسته‌شدن خودکار):** کلیک «پایان نشست» یا اتمام `MEETING_DURATION_MINUTES=60`؛ بسته‌شدن خودکار توسط scheduler هم با اتمام ۶۰ دقیقه و هم با **سکوت ۱۰ دقیقه** (`MEETING_IDLE_TIMEOUT_MINUTES=10`، ستون `last_msg_at` روی `meetings`/`group_meetings` در `_COLUMN_MIGRATIONS`).
- **رفع باگ قفل‌ماندن:** «پایان نشست» اکنون **همه‌ی** نشست‌های فعال/معلق کشور را می‌بندد (`_end_all_engagements_for_country`)، و میزبانِ نشست چندجانبه‌ی معلق هم در `get_active_engagement_label` به‌عنوان مشغول شناخته می‌شود (`get_hosted_group_meeting_for_country`).
- **نشست‌های اخیر:** دیدار حضوری → «🕘 نشست‌های اخیر» (`dip:meetings_recent`): فهرست نشست‌های دوجانبه/چندجانبه‌ی اخیر با وضعیت؛ نشست‌های باز دکمه‌ی «پایان» دارند (`meet_force_end:bi|grp:{id}`) تا کاربر از قفل خارج شود.
- **ریپلای در نشست:** رله‌ی پیام‌ها روی ریپلای کار می‌کند و در پیام رله‌شده سرخط «↩️ در پاسخ به …» می‌گذارد (`_reply_prefix`)؛ رله مقاوم است و در هر پیام `last_msg_at` را به‌روز می‌کند.
- **کامند ریست نشست‌ها:** `/resetmeetings` (فقط مالک) همه‌ی دیدارها/نشست‌ها/تماس‌های فعال یا معلق را می‌بندد (`dip_repo.close_all_active_meetings`) — برای رفع گیرکردن‌های موجود.
- **صندوق پستی (پاسخ):** در صندوق پستی برای هر نامه‌ی بی‌پاسخ دکمه‌ی «📩 پاسخ» جدا نمایش داده می‌شود؛ پس از پاسخ (`replied=True`) دکمه حذف می‌شود.
- **مدیریت اعضای اتحاد:** در «اتحاد من» برای مالک دکمه‌های «➕ افزودن کشور» (ارسال درخواست با پذیرش/رد: `alli_add_to`/`alli_join_ok`/`alli_join_no`) و «➖ حذف کشور» (`alli:remove`/`alli_rm`) اضافه شد. رعایت `ALLIANCE_MAX_MEMBERS` و عضویت یکتا.

## ویژگی‌های v1.11

- **محدودیت تأسیسات مشترک:** هر کشور در هر ۱۲ ساعت حداکثر `JOINT_BUILD_LIMIT=3` تأسیسات مشترک می‌سازد (چک در `joint.py:cb_joint_start` با `facilities.count_joint_requests_since` که درخواست‌های pending/done را در پنجره می‌شمارد).
- **محدودیت سرمایه‌گذاری:** هر کشور در هر ۱۲ ساعت حداکثر `INVESTMENT_LIMIT=2` سرمایه‌گذاری ثبت می‌کند (helper `_invest_limit_exceeded` با `investments.count_by_investor_since`؛ چک fail-fast در ورود به داخلی/خارجی و چک نهایی در `cb_invest_confirm`).
- **استقرار نیرو (فقط VIP):** بخش نظامی → «🪖 استقرار نیرو». فقط کشورهای `is_vip`. منو: «استقرار نیروی جدید» (انتخاب نوع کلان زمینی/دریایی/هوایی → قلم تجهیزات از آن دسته → تعداد → منطقه → تأیید) و «نیروهای مستقر» (آپدیت مکان / حذف گروه). هزینه‌ی **تصادفی نفت** بین ۱ تا ۱۰ میلیون بشکه وابسته به حجم نیرو (`_compute_oil_cost`، ثابت‌های `DEPLOY_OIL_*`)؛ فقط نفت کسر می‌شود (تجهیزات در موجودی می‌مانند). نگاشت سه دسته‌ی کلان به branchهای واقعی در `DEPLOY_BRANCHES`. با تأیید: خبر فوری با عکس مخصوص (`ground/navy/air` از `D:\PictureDB\Military`، کش‌شده در `File.md`) در **کانال نظامی** + لاگ. مدل `Deployment`؛ repo `deployments`؛ handler `deployment.py`؛ فرم `DeploymentForm`.
- **توسعه‌ی پنل `/god`:** افزوده شد «🚫 تحریم‌ها» (فهرست/لغو/افزودن)، «📈 سرمایه‌گذاری‌ها» (فهرست/حذف/افزودن)، «💥 سیستم تلفات» (کشور→نوع نیرو→قلم→عدد تلفات→کسر با `mil_repo.reduce_count`). در «🏭 تأسیسات و کارخانه‌ها» دکمه‌ی حذف هر تأسیسات/کارخانه + «افزودن تأسیسات»/«افزودن کارخانه» (god mode رایگان، بازدهی تأسیسات از `economy_service.facility_yield_for`). ورودی‌های متنی/عددی از طریق `GodForm.entering_value` با `god_kind`های جدید (`casualty`, `facadd_loc`, `mfadd_name`, `mfadd_loc`, `invadd_amount`).

## سیستم تصویری منوهای اصلی (v1.12)

- **۷ منوی اصلی عکس‌دار:** پنل اصلی، اقتصاد، دیپلماسی، نظامی، مشاور، وضعیت کشور، بانک — هر بار یک عکس تصادفیِ مخصوص همان صفحه از فولدر `D:\PictureDB\UI\<name>` (main/economy/diplomacy/military/advisor/status/bank) نمایش می‌دهند.
- **هیبرید:** صفحات کوتاه (≤۱۰۲۴ کاراکتر) = عکس + کپشن + دکمه؛ صفحات بلندتر خودکار فقط متن (سقف کپشن تلگرام).
- **دو هلپر مرکزی در `bot/utils/screens.py`:**
  - `show_menu(event, text, reply_markup, image_key)` — منوی اصلی را عکس‌دار رندر می‌کند (event = CallbackQuery یا Message). عکس تازه می‌فرستد و پیام قبلی را حذف می‌کند؛ اگر عکس نبود یا متن بلند بود، به متن fallback می‌کند.
  - `safe_edit(call, text, reply_markup, **kwargs)` — **جایگزین امنِ `call.message.edit_text`**. روی پیام عکس‌دار (که با `edit_text` نمی‌شود به متن تبدیلش کرد) پیام را حذف و متن تازه می‌فرستد؛ روی پیام متنی دقیقاً مثل `edit_text` است. **قاعده:** در هندلرها به‌جای `call.message.edit_text` از `safe_edit(call, ...)` استفاده کن تا گذار عکس→متن نشکند (استثنا: ویرایش پیام کاربر دیگر مثل `target.message.edit_text` و ضمیمه‌کردن به پیام درخواست/لاگ).
- **`media.py`:** هفت کلید `ui_*` در `MEDIA_DIRS` + `UI_CATEGORIES` + تابع مشترک `_resolve_random_photo(category)`. کش `file_id` مثل بقیه در `File.md`.
- **پیش‌کش:** `python -m scripts.cache_media` دسته‌های `ui_*` را هم آپلود و کش می‌کند (برای VPS بدون فولدر محلی).
- **fallback کامل:** فولدر خالی/غایب → صفحه بی‌عکس، بدون کرش. عکس‌ها را می‌توان تدریجی اضافه کرد.

## ویژگی‌های v2.0 (آپدیت جامع ماهواره، پایگاه‌های نظامی و بازسازی سیستم جنگ)

- **پایگاه‌های نظامی (فقط VIP):** بخش نظامی → «🏗 پایگاه‌های نظامی». منوی احداث پایگاه (هوایی/زمینی/دریایی). احداث در خاک خود یا ارسال درخواست به میزبان خارجی. انتقال و مدیریت تجهیزات در پایگاه با `base_service`. مدل‌های `MilitaryBase` و `BaseEquipment`.
- **ماهواره‌های جاسوسی فضایی (فقط VIP):** بخش نظامی → «📡 ماهواره فضایی». پرتاب ماهواره با نرخ شانس بر اساس قدرت اقتصادی. ۳۰ دقیقه پرواز تا مدار (`in_orbit`). قابلیت «رصد جاسوسی» کامل از پایگاه‌ها، تجهیزات و نیروهای مستقر کشور هدف. خبر فوری پرتاب/انفجار در کانال نظامی. مدل `Satellite` و سرویس `satellite_service`.
- **اعلام جنگ دیپلماتیک:** بخش دیپلماسی → «🚨 اعلام جنگ». الزامی برای حملات علنی زمینی/هوایی/دریایی. مدل `WarDeclaration` و سرویس `battle_service`.
- **بازسازی سیستم نبرد و تأیید مالک:** حملات با ۴ نوع (زمینی، هوایی، دریایی، خرابکاری). درخواست حمله با وضعیت `pending_owner` ثبت و برای تأیید اولیه به لاگ مدیریت ارسال می‌شود. با کلیک مالک روی `✅ تأیید و شروع نبرد` وارد وضعیت `in_progress` شده و فازهای نبرد اجرا می‌شوند.
- **اخبار پویای ۴ مرحله‌ای نبرد با عکس:** پردازش نبردهای فعال توسط زمان‌بند در ۴ فاز متوالی زمانی: فاز ۱ (فلش فوری)، فاز ۲ (درگیری دفاعی/پدافند)، فاز ۳ (گزارش خسارات) و فاز ۴ (خلاصه نهایی + کسر خودکار تلفات و اثرات اقتصادی). تمامی ۴ فاز با عکس‌های پویا نظامی از طریق `send_photo_news` در کانال اخبار نظامی منتشر می‌شوند.

## سیستم توسعه‌ی هسته‌ای (v1.10.4)

- **دسترسی:** بخش نظامی → «☢️ تأسیسات هسته‌ای» — **فقط کشورهای VIP** (`is_vip`). ولی **اورانیوم برای همه** قابل استخراج است (معدن معمولی با `resource=uranium`) و در WTO قابل فروش؛ کشور غیر VIP می‌تواند تأمین‌کننده‌ی اورانیوم باشد.
- **منبع جدید:** `ResourceType.URANIUM` (واحد: تن، ☢️). ذخایر واقع‌گرایانه برای ۳۶ کشور در `countries.json` (۳۰ کشور `can_extract=true` طبق واقعیت). برای دیتابیس موجود: `python -m scripts.seed --refresh-reserves` (فقط ردیف‌های **جدید** را اضافه می‌کند و ذخایر موجود را دست نمی‌زند).
- **مقیاس زمانی:** هر «روز» سند اسپک = **۱۲ ساعت واقعی** (`NUCLEAR_DAY_HOURS=12`) → کل مسیر فاز ۱ تا ۵ حدود ۶ روز.
- **۵ فاز (`NuclearPhase`):** استخراج/آسیاب ← تبدیل UF6 ← غنی‌سازی ← تسلیحاتی‌سازی ← آزمایش. ارتقای فاز فقط رو به جلو (`_bump_phase`).
- **زنجیره‌ی سوخت واقعی:** سنگ اورانیوم → (آسیاب) کیک زرد U₃O₈ → (تبدیل) گاز UF6 → غنی‌سازی. **اصلاح اسپک:** خروجی فاز ۱ کیک زرد است نه UF6 (UF6 محصول فاز ۲ است).
- **غنی‌سازی با مدل SWU:** ۴ رده (۳.۵٪ → ۲۰٪ → ۶۰٪ → ۹۰٪) در `ENRICHMENT_TIERS`؛ هر رده از محصول رده‌ی قبل تغذیه می‌شود (رده‌ی اول از UF6). سرعت = تعداد سانتریفیوژ × `CENTRIFUGE_SWU_PER_UNIT_PER_24H` ÷ SWU لازم هر کیلوگرم. خوراک که تمام شود، زمان‌بند خودکار متوقف می‌کند.
- **سانتریفیوژ:** چرخه‌های ۵۰۰تایی (`CENTRIFUGE_BATCH_*`) با بودجه + فولاد + آلومینیوم در ۶ ساعت؛ ظرفیت هر سالن غنی‌سازی `ENRICHMENT_HALL_CENTRIFUGE_CAPACITY`. سالن آسیب‌دیده ظرفیتش به نسبت `integrity_pct` کم می‌شود.
- **کلاهک:** هر کلاهک `WARHEAD_HEU_REQUIRED_KG=25` کیلوگرم HEU ۹۰٪ + آزمایشگاه تسلیحاتی + فناوری فیزیک محاسباتی. قدرت تصادفی ۱۵ تا ۱۵۰ کیلوتن. نصب روی موشک بالستیک/بمب‌افکن/زیردریایی.
- **شاخص افشا (۰–۱۰۰):** **اصلاح اسپک** — «ریسک افشا»ی هر فاز به‌جای درصد تک‌عددی، یک **کف** شاخص است (۱۰/۲۵/۵۰/۷۵/۱۰۰) و اقدامات (ساخت، غنی‌سازی بالا، مونتاژ) روی آن انباشته می‌شوند. هر تیک، تاس کشف با `NUCLEAR_DETECTION_CHANCE_FACTOR` زده می‌شود؛ با کشف، «گزارش آژانس» در کانال دیپلماسی منتشر می‌شود.
- **پنهان‌کاری:** تأسیسات **زیرزمینی** (گران‌تر/کندتر، افشای کمتر، نصف خسارت در حمله، **از دید ماهواره پنهان**)، **پوشش صلح‌آمیز** (افشای کمتر در عوض سقف غنی‌سازی ۲۰٪)، **عملیات ضدجاسوسی** (هر ۲۴ ساعت، کاهش شاخص افشا)، **خروج از NPT** (خبر دیپلماتیک + افشای سنگین).
- **آزمایش هسته‌ای (فاز ۵):** تأیید دومرحله‌ای؛ کلاهک مصرف می‌شود، افشا ۱۰۰٪، +رضایت عمومی و −ثبات؛ خبر فوری عکس‌دار در کانال نظامی. **حمله‌ی هسته‌ای واقعی در بازی وجود ندارد** — سیستم فقط بازدارندگی و آزمایش است.
- **بازدارندگی:** در `battle_service.approve_battle_by_owner` زرادخانه‌ی مدافع تلفات و اثرات اقتصادیِ او را به نسبت `deterrence_defense_pct` (سقف ۳۰٪) کاهش می‌دهد — داخل try/except تا جریان نبرد نشکند.
- **اتصال‌ها:** رصد ماهواره‌ای تأسیسات سطحی را نشان می‌دهد و زیرزمینی‌ها را فقط به‌عنوان «فعالیت مشکوک» گزارش می‌کند؛ `apply_sabotage_damage` (خرابکاری سایبری سانتریفیوژ) و `apply_strike_damage` قلاب‌های آماده برای حمله؛ پنل `/god` → «☢️ برنامه‌های هسته‌ای» (افشا/مخفی‌سازی/صفرکردن افشا/توقف غنی‌سازی/حذف کامل با تأیید دومرحله‌ای)؛ ریست فصل همه‌ی ۶ جدول را پاک می‌کند؛ دسته‌ی عکس `nuclear` (`D:\PictureDB\Nuclear`) در `cache_media`.
- **فایل‌ها:** مدل‌ها `models/nuclear.py` (`NuclearProgram/Tech/Facility/Warhead/Test/Inspection` — همه با `create_all` خودکار، بدون نیاز به مهاجرت ستون)، repo `repositories/nuclear.py`، سرویس `services/nuclear_service.py`، هندلر `handlers/nuclear.py`، کیبورد `keyboards/nuclear.py`، فرم `states/nuclear.py`، زمان‌بند `process_nuclear` در `scheduler/jobs.py`.
- **تست:** `python -m scripts.test_nuclear_flow` — ۲۷ بررسی روی SQLite: کل فلوی فاز ۱ تا ۵ با جهش زمانی، پیش‌نیازها، سقف‌ها، پنهان‌کاری، خرابکاری و مسدودبودن دسترسی غیر VIP.

## بازسازی کامل سیستم نظامی (v1.10.6) — «ستاد فرماندهی کل»

سنگین‌ترین آپدیت بازی. سیستم حمله/نبرد از صفر بازنویسی شد؛ پایگاه، ماهواره،
استقرار، کارخانه، فروش و هسته‌ای حفظ و به سیستم جدید متصل شدند.

### 🗺 لایه‌ی جغرافیا (جدید)
- `data/geography.json` — برای هر ۳۶ کشور: همسایگان زمینی، دریاهای مشترک، قاره، مختصات پایتخت.
- `services/geo_service.py` — `are_neighbors`, `distance_km` (هاورساین), `distance_tier`
  (همسایه/منطقه‌ای/قاره‌ای/فراقاره‌ای), `shares_sea`, `is_landlocked`, `route_crosses`.
- `route_crosses` مسیر محموله را با «کریدور زمینی» + «مسیر دریایی» تعیین می‌کند
  (سقف انحراف: نسبی ۳۵٪ **و** مطلق ۱۲۰۰ کیلومتر) — پایه‌ی رهگیری محموله.

### ⚔️ موتور نبرد محاسباتی (جایگزین داوری AI)
**تغییر بنیادین:** نتیجه‌ی نبرد را کد تعیین می‌کند، نه هوش مصنوعی. AI فقط از روی
اعداد واقعی، متن خبر می‌نویسد. نتیجه: عادلانه، قابل‌تست، و مستقل از در دسترس بودن AI.

- `services/combat/profiles.py` — پروفایل قدرت ۱۸ دسته تجهیزات + ضریب کیفیت از روی
  نام (F-35 قوی‌تر از F-4، S-400 قوی‌تر از HAWK).
- `services/combat/power.py` — قدرت تهاجمی/پدافندی، نگاشت نوع عملیات به شاخه‌های مجاز.
- `services/combat/engine.py` — الگوریتم ۹ مرحله‌ای: امکان‌سنجی ← قدرت مؤثر (فاصله ×
  آمادگی × فرمانده) ← رهگیری پدافند ← تلفات مهاجم ← خسارت و تلفات مدافع ← بازدارندگی
  هسته‌ای ← تلفات غیرنظامی ← اثرات اقتصادی دوطرفه ← شدت و فازها.
- نتایج با `random.Random(seed)` بازتولیدپذیرند.

### 📰 موتور اخبار ضدتکرار
مشکل قبلی: یک پرامپت ثابت با ۴ فاز ثابت → اخبار تکراری. راه‌حل سه‌لایه:
- `news/archetypes.py` — ۸ سبک خبری (`flash`, `eyewitness`, `official`, `analyst`,
  `defense_report`, `damage_assess`, `wire`, `ticker`) که چرخشی انتخاب می‌شوند.
- `news/dedup.py` — اثرانگشت n-gram + شباهت ژاکارد؛ خبر با شباهت ≥۵۵٪ بازتولید می‌شود.
  نرمال‌سازی حروف عربی/فارسی و حذف ایموجی پیش از مقایسه.
- `news/facts.py` — تبدیل اعداد موتور نبرد به واقعیت‌های خوانا + قالب‌های پشتیبان
  (بدون AI هم خبر منتشر می‌شود و تکراری نیست).
- `news/military_news.py` — تا ۳ تلاش برای تولید خبر غیرتکراری، سپس پشتیبان.
- `news/publisher.py` — **فیلتر کانال:** خبر فقط وقتی به کانال نظامی می‌رود که یکی از
  طرفین VIP باشد و شدت ≥۵ (بدون VIP: شدت ≥۹). بقیه فقط پیوی طرفین + گروه لاگ.

### 🎖 منوی ستاد فرماندهی کل
`menu:military` حالا در `handlers/command_center.py` است (هندلر قدیمی از `menu.py` حذف شد).
هشت بخش: عملیات‌ها، وضعیت ارتش، دفاع و پدافند، اطلاعات و شناسایی، فرماندهان،
تاریخچه عملیات، پایگاه‌های نظامی، صنایع نظامی.

### 💥 سیستم عملیات
- **۴ نوع حمله:** زمینی، هوایی، دریایی، خرابکاری + ترور و رهگیری محموله.
- **انتخاب دقیق تجهیزات:** بازیکن قلم‌به‌قلم از موجودی واقعی انتخاب می‌کند و تعداد می‌زند؛
  تلفات دقیقاً از همان اقلام کم می‌شود.
- **پیش‌نمایش پیش از تأیید:** رهگیری تخمینی، خسارت تخمینی، هزینه‌ی سوخت و بودجه.
- **همه‌ی عملیات‌ها نیاز به تأیید مالک دارند** (دکمه در گروه لاگ).
- **سقف کلی:** `OPERATION_LIMIT_PER_WINDOW=3` در هر ۲۴ ساعت (گشت و رزمایش مستثنا).
- **مدت متغیر:** ۳ تا ۹ فاز خبری بر اساس شدت (۳ تا ۱۸ دقیقه).
- حملات علنی نیاز به **اعلام جنگ رسمی** دارند؛ عملیات مخفیانه نه.

### 🛩 گشت / 🎪 رزمایش / 🎯 ترور / ⚓ رهگیری
- **گشت** (`patrol_service`): بونوس رهگیری پدافند، شانس کشف خرابکاری/ترور، بونوس
  رهگیری محموله. سقف ۳ گشت هم‌زمان، مدت ۱۲ ساعت.
- **رزمایش** (`drill_service`): آمادگی رزمی (`countries.readiness`، سقف ۴۰) که مستقیماً
  قدرت نبرد را بالا می‌برد و روزانه ۳ واحد افت می‌کند. تکی یا مشترک (با پذیرش شریک).
- **ترور** (`assassination_service`): فرمانده NPC (بونوس شاخه تا ۴۸ ساعت از بین می‌رود)
  یا رئیس‌جمهور بازیکن (شانس ۸٪ → «بحران رهبری» ۶ ساعته، منع عملیات).
  گشت هدف می‌تواند عملیات را خنثی کند؛ شکست معمولاً افشا می‌شود.
- **رهگیری** (`interception_service`): فقط محموله‌هایی که `route_crosses` می‌گوید از
  قلمرو ما می‌گذرند. نتیجه: مصادره یا نابودی + بحران دیپلماتیک.

### 🎖 فرماندهان
- `data/commanders.json` + `scripts/seed_commanders.py` (۱۳۹ فرمانده برای ۳۶ کشور).
- هر فرمانده به شاخه‌ی تخصصی خود بونوس می‌دهد؛ کشور محصور در خشکی فرمانده دریایی نمی‌گیرد.
- ریست فصل خودکار فرماندهان را بازسازی می‌کند.

### ⏱ زمان‌بند (جاب‌های جدید)
`process_operations` (فازهای خبری)، `process_patrols`، `process_drills`،
`process_readiness_decay`، `process_commander_replacements`.
جاب قدیمی `process_battle_phases` حذف شد.

### 🖼 تولید عکس با Gemini (اختیاری)
- `services/ai/gemini_image.py` + `news/imagery.py` — پرامپت تصویری از واقعیت‌های نبرد.
- **بدون کلید API خودکار خاموش است** و روی بانک عکس محلی کار می‌کند — هیچ‌جا نمی‌شکند.
- تنظیمات در `.env`: `GEMINI_API_KEY`, `GEMINI_IMAGE_MODEL`, `GEMINI_IMAGE_DAILY_LIMIT`.
- کلید از https://aistudio.google.com/apikey (بیلینگ جدا از اشتراک Google AI Pro).

### 🛠 پنل مدیریت (`/god` → «⚔️ عملیات نظامی»)
`handlers/god_operations.py`: تأیید/رد عملیات، بستن فوری عملیات گیرکرده، مدیریت
گشت‌ها و رزمایش‌ها، ترور/احیای دستی فرماندهان.

### 🗑 حذف‌شده‌ها
`keyboards/battle.py`، جریان حمله در `handlers/battle.py` (فقط اعلام جنگ ماند)،
`battle_service.create_battle_request/approve_battle_by_owner/process_battle_phases`،
`AttackForm`، `attack_types_kb`.

### 🧪 تست (۱۴۶ بررسی، بدون نیاز به PostgreSQL/توکن/AI)
```bash
python -m scripts.test_geography        # ۲۹ بررسی — جغرافیا و مسیرها
python -m scripts.test_combat_engine    # ۴۵ بررسی — تعادل نبرد
python -m scripts.test_news_engine      # ۳۸ بررسی — ضدتکرار اخبار
python -m scripts.test_operation_flow   # ۳۴ بررسی — چرخه‌ی کامل عملیات
python -m scripts.test_nuclear_flow     # رگرسیون سیستم هسته‌ای
```

### 📌 راه‌اندازی پس از این آپدیت
```bash
python -m scripts.seed_commanders   # ساخت فرماندهان (یک‌بار)
```
ستون‌های جدید `countries` (`readiness`, `last_readiness_decay_at`,
`leadership_crisis_until`) خودکار توسط `init_db` اضافه می‌شوند.

## قواعد ظاهری UI (v1.8)

- رنگ دکمه‌ها از طریق فیلد `style` در `InlineKeyboardButton` (Bot API 9.4+) اعمال می‌شود.
  ثابت‌ها در `bot/utils/ui.py`: `STYLE_OK='success'` (سبز)، `STYLE_NO='danger'` (قرمز)، `STYLE_MAIN='primary'` (آبی).
- قرارداد رنگ‌ها: سبز = تأیید/پذیرش/خرید/فروش/احداث/انتخاب‌شده؛ قرمز = لغو/رد/حمله/وضع تحریم/پایان فصل/پایان تماس؛ آبی = دکمه‌های اصلی منو و کنش اصلی هر صفحه؛ بدون رنگ = فهرست‌ها و بازگشت.
- ایموجی نشانگر انتخاب: `PICK_ON='🟢'` / `PICK_OFF='⚪️'` (به‌عنوان fallback برای کلاینت‌های بدون پشتیبانی رنگ).
- سربرگ‌ها با `header(title, emoji)` ساخته می‌شوند (عنوان درشت + خط جداکننده `DIVIDER`).

## مرزها و احتیاط‌ها

- عملیات‌های مخرب (مثل `/endseason`) همیشه تأیید دومرحله‌ای داشته باشند.
- کلیدها و `.env` هرگز در گیت قرار نگیرند (در `.gitignore` هست).
- خطاهای ارسال تلگرام/AI نباید جریان بازی یا scheduler را متوقف کنند (همیشه `try/except` با لاگ).
